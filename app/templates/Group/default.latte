{* Latte template *}

{block content}

<div n:foreach="$groups as $group" class="jumbotron">
    <h3><a n:href="Group:detail, $group->id_groups">{$group->name}</a></h3>
    {*<p>{$group->text}</p>
    <small>Last activity at: {$group->created_dt}</small>*}
</div>
{if !$groups}
    <blockquote>
        <h2 n:if="!$iterations">You are not in any group!</h2>
    </blockquote>
{/if}